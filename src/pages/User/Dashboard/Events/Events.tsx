import EventsSection from "../../../../components/events/EventsSection";

// Same public browse-and-RSVP experience, just rendered inside the user's
// dashboard shell instead of the public /events page.
export default function Events() {
  return <EventsSection />;
}
