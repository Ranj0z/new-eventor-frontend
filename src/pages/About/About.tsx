export default function About() {
  return (
    <section className="max-w-3xl mx-auto px-4 py-16">
      <h1 className="font-display text-3xl md:text-4xl mb-4">About Eventor</h1>
      <p className="text-base-content/70 leading-relaxed">
        Eventor connects people with events happening in venues around them.
        Hosts list events, guests reserve a spot — with or without an
        account — and everything from check-in to payment happens in one
        flow instead of scattered across email and spreadsheets.
      </p>
    </section>
  );
}
